import { ActionTree } from 'vuex';
import { RootState, WimdoState } from './types';

export const actions: ActionTree<WimdoState, RootState> = {

    // addPage({ commit }): void {

    // },

    // addSentence({ commit }): void {

    // },

    // fetch_old_contest({ commit }): void {
    //     fetcher.get(`src/web/ContestController.php`, "get_old_contest_infos", {})
    //     .then(response => {
    //         commit("set_old_contest_infos", response.data)
    //     })
    // },

};
